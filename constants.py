"""
Defines the OUTPUT_PATH constant for use in `scraper` and `searcher` modules.
- OUTPUT_PATH: Root of filetree that contains MLP transcript files.
"""

from pathlib import Path

OUTPUT_PATH = Path("output/")
